Implement pagebox MVP according to docs/spec.md.

## Task

Implement all files listed in docs/spec.md in the order specified (steps 1-11). The spec contains complete file paths, function signatures, and implementation details — follow it exactly without creative deviation.

Key constraints:
- Working directory: /Users/yosuke/workspace/github.com/YosukeIida/pagebox
- Runtime: Bun (not Node). Use `bun add` not `npm install`.
- No build step: Bun runs TypeScript directly. Client-side TS is transpiled at runtime via Bun.Transpiler.
- Zero external service dependencies in MVP: storage = local filesystem, DB = SQLite file on volume.
- Do NOT import from adapters/ or db/ inside core/ or http/. Dependency direction is enforced.
- Run `bun run typecheck` after all files are created and fix any errors before finishing.

## Completion Conditions (ALL must be true)

1. `bun run typecheck` exits with code 0 (no TypeScript errors)
2. `bun run src/entries/bun.ts` starts without crashing and logs "pagebox listening on http://localhost:3000"
3. All source files listed in docs/spec.md under "ディレクトリ構成" exist under src/
4. deploy/docker/Dockerfile and deploy/docker/compose.yaml exist
5. docs/spec.md exists (already present — do not delete)
6. No file inside src/core/ or src/http/ contains an import path that includes "adapters" or "db/"
